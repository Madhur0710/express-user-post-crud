<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5" href="/logout">Logout</a>
       </div>
        <h1 class="text-3xl mb-3"><span class="font-light">Heyy</span>, <%= user.name %>🖐</h1>
        <h4 class="mb-5">You can create a new Post</h4>
        <form action="/post" method="post">
         <textarea placeholder="What's on your mind..." class="block resize-none bg-transparent w-1/3 border-2 outline-none rounded-lg p-2" name="content"> </textarea>
         <input class="px-3 py-2 w-40 bg-blue-500 cursor-pointer block rounded-md mt-3 text-sm" type="submit" value="Create New Post"/>
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5">
           <% user.posts.reverse().forEach(function(post){ %>
            <div class="post w-1/3 rounded-md p-4 mb-4 border-[1px] border-zinc-700 bg-zinc-800">
                <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                <p class="text-sm tracking-tight"><%= post.content %></p>
                <small class="mt-2 inline-block"><%= post.likes.length %> Likes</small>
                <div class="btns flex gap-4 mt-1">
                    <a class="text-blue-500" href="/like/<%= post._id %>">
                        <%= post.likes.indexOf(user._id) === -1? "Like" : "Unlike" %>
                    </a>
                    <a class="text-zinc-500" href="/edit/<%= post._id %>">edit</a>
                    <a class="text-zinc-500" href="/delete/<%= post._id %>">delete</a>
                </div>
            </div>
            <% }) %>
            
            </div>
        </div>
    </div>
</body>
</html>