<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-10 text-white">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm inline-block mb-5" href="/logout">Logout</a>
       </div>
        <h4 class="mb-5">Edit your Post</h4>
        <form action="/update/<%= post._id %>" method="post">
         <textarea placeholder="What's on your mind..." class="block resize-none bg-transparent w-1/3 border-2 outline-none rounded-lg p-2" name="content"><%= post.content %></textarea>
         <input class="px-3 py-2 w-40 bg-yellow-500 text-black cursor-pointer block rounded-md mt-3 text-sm" type="submit" value="Update Post"/>
        </form>
    </div>
</body>
</html>